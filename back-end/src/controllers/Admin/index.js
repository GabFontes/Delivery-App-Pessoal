const destroy = require('./DestroyUserController');
const create = require('./CreateUserController');
const getAll = require('./GetAllController');

module.exports = {
  destroy,
  create,
  getAll,
};
